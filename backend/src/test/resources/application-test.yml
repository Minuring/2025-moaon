spring:
  application:
    name: backend

  flyway:
    enabled: false

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  elasticsearch:
    uris: http://localhost:9200

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: trace

google:
  clientId: abcd
  clientSecret: abcd
  redirectUri: abcd

jwt:
  secret: g/DGwpKK2nrqQC2lWweIWX+od76/qs6mMZIpz6OKd0ogo0HLkMOs5dkG9dLUtKtN65xjcgnPMJNrzyGTiCjA/g==

s3:
  bucket: testbucket
  region: testregion

gpt:
  api-key: testKey
  model: google/gemini-2.5-flash
  connect-timeout-millis: 1000
  read-timeout-millis: 5000

